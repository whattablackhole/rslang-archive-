import { DeactivateCardDirective } from './deactivate-card.directive';

describe('Directive: DeactivateCard', () => {
  it('should create an instance', () => {
    const directive = new DeactivateCardDirective();
    expect(directive).toBeTruthy();
  });
});
