import { FilterByActionPipe } from './filter-by-action.pipe';

describe('FilterByActionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByActionPipe();
    expect(pipe).toBeTruthy();
  });
});
