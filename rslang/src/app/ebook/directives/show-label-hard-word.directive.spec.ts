import { ShowLabelHardWordDirective } from './show-label-hard-word.directive';

describe('ShowLabelHardWordDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowLabelHardWordDirective();
    expect(directive).toBeTruthy();
  });
});
